# infra/docker

Dockerfile e configurazioni container per API, worker e admin.

Popolato nella **fase 08** (`docs/prompts/08_coolify_deploy.md`): build multi-stage,
utente non-root, immagini ridotte, lockfile frozen.
