/**
 * Il modello SearchEngine vive in @businessbox/contracts (schema Zod condiviso
 * con shell e IPC); qui viene solo ri-esportato per comodità del package.
 */
export type { SearchEngine, SearchSettings } from "@businessbox/contracts";
